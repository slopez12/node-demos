'use strict';

// var user;
const user;

user = { name: "Sara", age: 22 };

if(true) {
  var dog = "Mr.wag"
}

const colors = ['red', 'green', 'blue'];

for (var j = 0; j < colors.length; i++) {
  console.log(color);
}
